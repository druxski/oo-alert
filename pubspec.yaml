name: olx_otomoto_alerts
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.4.1
  fl_chart: ^1.0.0
  material_color_utilities: ^0.13.0
  meta: ^1.17.0
  vector_math: 2.1.4  # Zmieniono na wersję zgodną z flutter_test
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true